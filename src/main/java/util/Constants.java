package util;

/** This class contains the constants used by the web server
 * */
public class Constants {

    //the message that will be returned if an error occurs during parsing
    public static final String ERROR_RESPONSE = "error";
    //the message that will be returned if no error occurs during parsing
    public static final String OK_RESPONSE = "ok";
}
